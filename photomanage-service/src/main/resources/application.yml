spring:
    application:
        name: photomanage-service
    config:
        import: nacos:photomanage-service-dev
    cloud:
        nacos:
            discovery:
                server-addr: localhost:8848
            config:
                server-addr: localhost:8848
                file-extension: yml
